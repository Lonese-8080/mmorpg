# Grafana 仪表盘自动配置

apiVersion: 1

providers:
  - name: 'MMORPG Framework'
    orgId: 1
    folder: 'Framework'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
